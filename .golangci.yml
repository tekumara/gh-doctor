linters:
  disable-all: true
  enable:
    # default see https://golangci-lint.run/usage/linters/#enabled-by-default
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    # non-default
    - gocritic
    - revive
